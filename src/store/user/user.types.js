// Here, we have all the possible action types. Anything else, should fall into the 
// default case at the reducer switch.
export const USER_ACTION_TYPES = {
    SET_CURRENT_USER: 'user/SET_CURRENT_USER'
};
